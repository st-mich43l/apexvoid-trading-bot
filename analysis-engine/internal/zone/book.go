package zone

// Book is the zone-domain slice of a symbol's canonical state. See
// structure.Book's doc comment — same reservation purpose.
type Book struct {
	// SupplyDemand, OrderBlocks, FVGs, Breakers, FlipZones: added once
	// internal/zone has real types (Stage 2 of
	// docs/go-analysis-migration-audit.md).
}
